{
  "name": "obsidian-static-site",
  "version": "1.0.0",
  "description": "Simple static site generator for Obsidian markdown files",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "serve": "npx serve dist -p 3000",
    "dev": "npm run build && npm run serve"
  },
  "keywords": ["obsidian", "markdown", "static-site", "recipes"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "marked": "^12.0.1",
    "front-matter": "^4.0.2"
  }
}
